to make dbschema:

1. create a database on conig
2. make controller(p controller, etc)---for crud operation
3. make model (p.js, cat.js etc)--- for we define schema(name email etc)
4. make route (authroute.js ect)--- for define route (authcontroller route---define in product route and product route---app.js and ----run)
